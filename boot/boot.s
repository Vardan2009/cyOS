BITS 32

MAGIC EQU 0x1BADB002
FLAGS EQU 0x00000000

SECTION .multiboot
	ALIGN 4
	DD MAGIC
	DD FLAGS
	DD -(MAGIC + FLAGS)

SECTION .text
GLOBAL start
EXTERN kmain

start:
	CLI
	MOV ESP, STACKSPACE
	CALL kmain
	HLT
HALTKERN:
	CLI
	HLT
	JMP HALTKERN

SECTION .bss
RESB 8192
STACKSPACE:
